# Railway deployment configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd services/backend && python -m uvicorn main_minimal:app --host 0.0.0.0 --port $PORT --workers 1"
restartPolicyType = "ON_FAILURE"